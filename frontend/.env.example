# Base URL of the Strapi backend.
#
# Public because the browser needs it for absolute links; server components and
# server actions read the same value. In production this is the Railway URL,
# with no trailing slash.
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337
