# Base URL of the Strapi backend.
# Public because the browser calls Strapi directly for some requests;
# server components and route handlers use the same value.
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337
